# The Librarian — Core Runtime Dependencies
# (auto-installed by librarian_cli.py on first boot in Cowork VM)
# These are lightweight and install on any VM with space constraints.

# Core
anthropic>=0.40.0

# CLI
rich>=13.0.0

# Numeric (usually pre-installed on Cowork VMs)
numpy>=1.24.0
